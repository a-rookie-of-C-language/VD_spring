server:
  port: 8080
  servlet:
    context-path: /api
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json,image/svg+xml
  tomcat:
    threads:
      max: 400
      min-spare: 50
    accept-count: 200
    max-connections: 8192
    connection-timeout: 5000

spring:
  web:
    resources:
      chain:
        strategy:
          content:
            enabled: true
        cache: true
      cache:
        cachecontrol:
          max-age: 30d
          cache-public: true
          must-revalidate: true
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/VD?allowPublicKeyRetrieval=TRUE&useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&cachePrepStmts=true&prepStmtCacheSize=250&prepStmtCacheSqlLimit=2048&useServerPrepStmts=true&rewriteBatchedStatements=true}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
      connection-timeout: 5000
      idle-timeout: 300000
      max-lifetime: 600000
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 20MB
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: ${SPRING_RABBITMQ_PORT:5672}
    username: ${SPRING_RABBITMQ_USERNAME:guest}
    password: ${SPRING_RABBITMQ_PASSWORD:guest}
    publisher-confirm-type: correlated
    publisher-returns: true
    template:
      mandatory: true
  flyway:
    enabled: true
    baseline-on-migrate: true
  # Mail configuration for alerts
  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:25}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
    default-encoding: UTF-8

# Actuator configuration for metrics
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info,env
  endpoint:
    health:
      show-details: always
  simple:
    metrics:
      export:
        enabled: true

app:
  upload:
    base-path: uploads
    cover-path: covers
  security:
    jwt:
      secret: ${JWT_SECRET:dev-only-change-me-jwt-secret-32-chars}
      access-expiry-minutes: 15
      refresh-expiry-minutes: 10080
  messaging:
    dev-mode-trigger: true
  logging:
    service-name: volunteer-duration
    environment: dev
    elk:
      host: localhost
      port: 5000
    es:
      scheme: http
      host: localhost
      port: 9200
      index-pattern: volunteer-duration-*
      business-index-prefix: volunteer-business-log
      business-index-template: volunteer-business-log-template
      business-ilm-policy: volunteer-business-log-ilm
      ilm-retention-days: 30
      business-shards: 1
      business-replicas: 1
      fallback-buffer-size: 2000
      fallback-flush-batch-size: 200
      fallback-flush-ms: 5000
  monitoring:
    ws:
      max-connections: 2000
      sample-rate: 1
      degrade-threshold: 1500
    sse:
      max-connections: 500

mybatis:
  configuration:
    lazy-loading-enabled: true
    aggressive-lazy-loading: false

logging:
  level:
    root: INFO
    site.arookieofc: INFO
